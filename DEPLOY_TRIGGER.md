# Render Deploy Trigger

This file exists only to trigger a fresh Render deployment after adding the data audit detail route.

Expected runtime route after deployment:

- `GET /data/audit/stale-symbols`
